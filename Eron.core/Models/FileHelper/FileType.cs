namespace Eron.core.Models.FileHelper
{ 
    public enum FileType
    {
        Image,
        ImageThumbnail,
        Background,
        BackgroundThumbnail,
        CategoryImage,
        CategoryBackground,
        GalleryItem,
        GalleryThumbnail
    }
}